<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="board">
	<insert id="write" parameterType="board">
		insert into board(board_no, board_ref, board_lev, board_seq, board_writer, 
		                  board_title, board_content, board_viewcnt, board_regdate, 
		                  board_modidate, board_deldate, user_id, commentcount)
		values (board_no_seq.nextval, board_no_seq.currval, 0, 0, #{board_writer}, 
				#{board_title}, #{board_content}, 0, to_char(sysdate, 'YYYY-MM-DD HH:MM:SS'),
				to_char(sysdate, 'YYYY-MM-DD HH:MM:SS'), 'notdeleted', '배영철', 0)
	</insert>

</mapper>